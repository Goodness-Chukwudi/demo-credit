
enum BIT {
    ON = 1,
    OFF = 0
}

enum USER_STATUS {
    ACTIVE = "active",
    DEACTIVATED = "deactivated",
    DELETED = "deleted",
    PENDING = "pending",
}

enum PASSWORD_STATUS {
    ACTIVE = "active",
    DEACTIVATED = "deactivated",
    DELETED = "deleted"
}

enum GENDER {
    MALE = "male",
    FEMALE = "female"
}

export {
    BIT,
    USER_STATUS,
    PASSWORD_STATUS,
    GENDER
}